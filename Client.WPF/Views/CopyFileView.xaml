<Window x:Class="Client.WPF.Views.CopyFileView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStyle="ToolWindow"
        Title="CopyFileView" Height="120" MinHeight="120" MaxHeight="120" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Label>From</Label>
            <Label Content="{Binding From}"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Label>To</Label>
            <Label Content="{Binding To}"/>
        </StackPanel>
        <TextBox Grid.Row="2" Text="{Binding Progress}"/>
    </Grid>
</Window>
